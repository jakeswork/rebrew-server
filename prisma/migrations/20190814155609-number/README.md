# Migration `20190814155609-number`

This migration has been generated by Jake Flynn at 8/14/2019, 3:56:09 PM.
You can check out the [state of the datamodel](./datamodel.prisma) after the migration.

## Database Steps

```sql
PRAGMA foreign_keys=OFF;

CREATE TABLE "lift"."new_Review"("id" TEXT NOT NULL  ,"userId" TEXT NOT NULL DEFAULT '' ,"beerId" INTEGER NOT NULL DEFAULT 0 ,"comment" TEXT NOT NULL DEFAULT '' ,"rating" REAL NOT NULL DEFAULT 0 ,PRIMARY KEY ("id"));

INSERT INTO "new_Review" ("id","userId","beerId","comment","rating") SELECT "id","userId","beerId","comment","rating" from "Review"

DROP TABLE "lift"."Review";

ALTER TABLE "lift"."new_Review" RENAME TO "Review";

PRAGMA "lift".foreign_key_check;

PRAGMA foreign_keys=ON;
```

## Changes

```diff
diff --git datamodel.mdl datamodel.mdl
migration 20190814152912-string-id..20190814155609-number
--- datamodel.dml
+++ datamodel.dml
@@ -21,8 +21,8 @@
 model Review {
   id      String @default(cuid()) @id @unique
   userId  String
-  beerId  String
+  beerId  Int
   comment String
   rating  Float
 }
```

## Photon Usage

You can use a specific Photon built for this migration (20190814155609-number)
in your `before` or `after` migration script like this:

```ts
import Photon from '@generated/photon/20190814155609-number'

const photon = new Photon()

async function main() {
  const result = await photon.users()
  console.dir(result, { depth: null })
}

main()

```
