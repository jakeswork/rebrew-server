# Migration `20190812160103-rename-user-model`

This migration has been generated by Jake Flynn at 8/12/2019, 4:01:03 PM.
You can check out the [state of the datamodel](./datamodel.prisma) after the migration.

## Database Steps

```sql
PRAGMA foreign_keys=OFF;

CREATE TABLE "lift"."new_User"("id" TEXT NOT NULL  ,"userName" TEXT NOT NULL DEFAULT '' ,"displayName" TEXT   ,"password" TEXT NOT NULL DEFAULT '' ,PRIMARY KEY ("id"));

INSERT INTO "new_User" ("id","password") SELECT "id","password" from "User"

DROP TABLE "lift"."User";

ALTER TABLE "lift"."new_User" RENAME TO "User";

PRAGMA "lift".foreign_key_check;

PRAGMA foreign_keys=ON;

CREATE UNIQUE INDEX "lift"."User.userName._UNIQUE" ON "User"("userName")
```

## Changes

```diff
diff --git datamodel.mdl datamodel.mdl
migration 20190812120039-added-password--deleted-posts..20190812160103-rename-user-model
--- datamodel.dml
+++ datamodel.dml
@@ -12,9 +12,9 @@
   provider = "nexus-prisma"
 }
 model User {
-  id    String  @default(cuid()) @id @unique
-  email String  @unique
-  name  String?
-  password String
-}
+  id          String  @default(cuid()) @id @unique
+  userName    String  @unique
+  displayName String?
+  password    String
+}
```

## Photon Usage

You can use a specific Photon built for this migration (20190812160103-rename-user-model)
in your `before` or `after` migration script like this:

```ts
import Photon from '@generated/photon/20190812160103-rename-user-model'

const photon = new Photon()

async function main() {
  const result = await photon.users()
  console.dir(result, { depth: null })
}

main()

```
