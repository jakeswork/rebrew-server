# Migration `20190814120112-added-review-model`

This migration has been generated by Jake Flynn at 8/14/2019, 12:01:12 PM.
You can check out the [state of the datamodel](./datamodel.prisma) after the migration.

## Database Steps

```sql
CREATE TABLE "lift"."Review"("id" TEXT NOT NULL  ,"userId" TEXT NOT NULL DEFAULT '' ,"beerId" TEXT NOT NULL DEFAULT '' ,"comment" TEXT NOT NULL DEFAULT '' ,"rating" REAL NOT NULL DEFAULT 0 ,PRIMARY KEY ("id"));

CREATE UNIQUE INDEX "lift"."Review.id._UNIQUE" ON "Review"("id")
```

## Changes

```diff
diff --git datamodel.mdl datamodel.mdl
migration 20190812160103-rename-user-model..20190814120112-added-review-model
--- datamodel.dml
+++ datamodel.dml
@@ -16,5 +16,13 @@
   id          String  @default(cuid()) @id @unique
   userName    String  @unique
   displayName String?
   password    String
+}
+
+model Review {
+  id      String @default(cuid()) @id @unique
+  userId  String
+  beerId  String
+  comment String
+  rating  Float
 }
```

## Photon Usage

You can use a specific Photon built for this migration (20190814120112-added-review-model)
in your `before` or `after` migration script like this:

```ts
import Photon from '@generated/photon/20190814120112-added-review-model'

const photon = new Photon()

async function main() {
  const result = await photon.users()
  console.dir(result, { depth: null })
}

main()

```
